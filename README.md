

# 📦 **Redis Docker Container**

![Redis Logo](https://upload.wikimedia.org/wikipedia/en/6/6b/Redis_Logo.svg)

---

## 📝 **Описание**

Этот Docker-контейнер предоставляет готовую к использованию сборку **Redis** — высокопроизводительного in-memory key-value хранилища.
Контейнер подходит для разработки, тестирования и продакшен-проектов.

> 💡 **Redis** широко применяется для кэширования, pub/sub-механизмов и хранения быстрых данных.

---

## 🔧 **Поддерживаемые аргументы**

| Аргумент Docker image | Описание                                             |
| --------------------- | ---------------------------------------------------- |
| `REDIS_PORT`          | Порт Redis внутри контейнера (по умолчанию **6379**) |
| `REDIS_PASSWORD`      | Пароль для аутентификации Redis                      |
| `APP_ENV`             | Среда запуска (`dev`, `prod`)                        |
| `MAXMEMORY`           | Максимально используемая память Redis                |

---

## ▶️ **Как запустить контейнер**

Запуск Redis в Docker:

```bash
docker run -d \
  --name my-redis \
  -p 6379:6379 \
  -e REDIS_PASSWORD=MyPass \
  -e APP_ENV=dev \
  my-redis-image
```

---

## ⚙️ **Настройка через переменные окружения**

Используйте переменные окружения для гибкой конфигурации контейнера:

### 🔹 Основные настройки

* `REDIS_PASSWORD` — **установка пароля** для Redis
* `REDIS_PORT` — порт, на котором сервис будет доступен
* `APP_ENV` — выбор *окружения* запуска

### 🔹 Настройки памяти

* `MAXMEMORY` — значение максимального использования RAM
* `MAXMEMORY_POLICY` — политика очистки (например: `allkeys-lru`)

---

## 💾 **Пример использования в Docker Compose**

```yaml
version: "3.8"

services:
  redis:
    image: my-redis-image
    container_name: redis-compose
    ports:
      - "6379:6379"
    environment:
      REDIS_PASSWORD: strong_pass
      APP_ENV: prod
      MAXMEMORY: 256mb
```

---

## 🔗 Полезные ссылки

* 📘 Официальная документация Redis:
  [https://redis.io/docs](https://redis.io/docs)

* 🐳 Docker Hub Redis:
  [https://hub.docker.com/_/redis](https://hub.docker.com/_/redis)

---

## 💬 Цитата

> “Redis is simple, fast and powerful — the perfect tool for modern distributed systems.”

---



